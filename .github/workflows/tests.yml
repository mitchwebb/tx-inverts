name: Python Tests

on: [push, pull_request]

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3

            - name: Install system dependencies
              run: |
                  sudo apt-get update 
                  sudo apt-get install -y --fix-missing gdal-bin libgdal-dev libpq-dev

            - name: Set up Python
              uses: actions/setup-python@v4
              with:
                  python-version: '3.13.2'

            - run: pip install -r ./backend/github_requirements.txt
            - run: pytest ./backend
              env:
                  # GLOBAL APP
                  ENV: ${{secrets.ENV}}
                  GBIF__USER: ${{secrets.GBIF__USER}}
                  GBIF__EMAIL: ${{secrets.GBIF__USER}}
                  GBIF__PASSWORD: ${{secrets.GBIF__USER}}

                  # PostgreSQL
                  DATABASE__USER: ${{secrets.GBIF__USER}}
                  DATABASE__PASSWORD: ${{secrets.GBIF__USER}}
                  DATABASE__HOST: ${{secrets.GBIF__USER}}
                  DATABASE__PORT: ${{secrets.GBIF__USER}}
                  DATABASE__NAME: ${{secrets.GBIF__USER}}

                  # CORS
                  CORS__DOMAIN: ${{secrets.GBIF__USER}}
